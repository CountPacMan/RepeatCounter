<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/styles.css">
  <title>Repeat Counter</title>
</head>
<body>
  <div class="container">
    <h1>Repeat Counter</h1>
    <h4>Occurences of <span class="highlight">{{ needle }}</span> in <span class="highlight">{{ haystack }}</span> = <strong>{{ count }}</strong></h4>
  </div>
  <form class="center" action="/">
    <button type="submit" class="btn-primary">Return Home</button>
  </form>
</body>
</html>
